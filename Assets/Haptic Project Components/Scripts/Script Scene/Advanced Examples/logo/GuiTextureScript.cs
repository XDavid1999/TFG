using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class GuiTextureScript : MonoBehaviour {

	private float width;
	private float height;

	// Use this for initialization
	void Start () 
	{	
		//width = GetComponent<Image>().preferredWidth;

		//height = GetComponent<Image>().preferredHeight;

		//GetComponent<Image>().PixelAdjustPoint = new Rect(width * 0.05f, Screen.height - height * 1.05f, width, height);
	}
}
